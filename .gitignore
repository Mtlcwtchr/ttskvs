# Генерируемое зеркало расширения TTS Editor. При каждой загрузке игры
# onLoadGame делает clearOutputPath() и пересоздаёт эти каталоги из скрипта,
# пришедшего из TTS — версионировать их нет смысла. Исходники живут в src/.
.tts/bundled/
.tts/output/
.tts/library/
