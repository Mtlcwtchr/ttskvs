<!-- Корень Global UI. <Defaults> идёт первым и задаёт базу для всех элементов
     ниже (api.tabletopsimulator.com/ui/defaults): без него TTS красит текст
     кнопок и полей в #323232 и прижимает его в левый верхний угол.

     Раскладка 4A: рейка партии у левого края, окно листа правее и ниже —
     сверху остаётся полоса под «порядок хода», справа место под панель броска
     и лог (этих элементов у нас пока нет). -->
<Box width="fill" height="fill">
  <Defaults>
    <Text color="textBody" font="CormorantGaramond" horizontalOverflow="Overflow" verticalOverflow="Truncate"
          resizeTextForBestFit="false"/>
    <Button colors="buttonStates" font="CormorantGaramond" textColor="buttonText" textAlignment="MiddleCenter"
            transition="ColorTint"/>
    <InputField colors="inputStates" font="CormorantGaramond" textColor="inputText" caretColor="caret"/>
  </Defaults>

  <Anchor at="topleft" x="22" y="130" width="100" height="auto">
    <PartyRail/>
  </Anchor>

  <CharacterSheet/>
</Box>
