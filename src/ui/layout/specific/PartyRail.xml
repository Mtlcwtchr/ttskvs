<!-- Полоска портретов партии у левого края (4A). Ширину и место задаёт
     <Anchor> в MainUI.xml; {{PORTRAITS}} — единственный слот: длину списка
     разметка знать не может. -->
<Col width="fill" height="auto" color="{{COLOR}}" padding="10" gap="12" align="top">
  {{PORTRAITS}}
  <AddCharacterButton size="{{SEAT_SIZE}}"/>
</Col>
