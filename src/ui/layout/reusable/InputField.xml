<!-- Поле правки. colors/textColor обязательны: по умолчанию TTS рисует поле
     БЕЛЫМ с тёмно-серым текстом (#323232) — на тёмном листе это белая заплатка.
     lineType — только SingleLine/MultiLineSubmit/MultiLineNewLine; прежнее
     "MultiLine" было невалидным значением и молча игнорировалось.
     resizeTextForBestFit: сжимает текст, если не влезает (minSize не менее 8pt).
     padding добавлен для вертикального выравнивания текста.
     Overflow на обе оси: текст не обрезается ни горизонтально, ни вертикально. -->
<InputField id="{{ID}}" width="{{WIDTH}}" height="{{HEIGHT}}" font="Arial" fontSize="{{FONT_SIZE}}"
            colors="{{COLORS}}" textColor="{{TEXT_COLOR}}" caretColor="caret"
            padding="2 4 2 4"
            horizontalOverflow="Overflow" verticalOverflow="Overflow"
            onEndEdit="{{ON_END_EDIT}}" onValueChanged="{{ON_VALUE_CHANGED}}"
            characterValidation="{{VALIDATION}}"
            resizeTextForBestFit="true" resizeTextMinSize="8"
            interactable="true" readOnly="false" lineType="{{LINE_TYPE}}" text="{{VALUE}}" />
